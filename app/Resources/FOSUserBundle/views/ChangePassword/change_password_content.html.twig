{% trans_default_domain 'FOSUserBundle' %}

<h3 class="mt-5 ml-5 pl-3">Modifiez votre mot de passe</h3>

<div class="container mt-2 ml-5 pl-5">

    {{ form_start(form, { 'action': path('fos_user_change_password'), 'attr': { 'class': 'fos_user_change_password' } }) }}
        {#{{ form_row(form) }}#}

        <div class="form-group">{{ form_widget(form, {'attr': {'class':'form-control col-sm-10'}})  }}</div>
        {#<div class="form-group">{{ form_row(form.new_password, {'attr': {'class':'form-control'}})  }}</div>#}

        <div>
            <input class="btn btn-primary btn-lg" type="submit" value="{{ 'profile.edit.submit'|trans }}" />
        </div>
    {{ form_end(form) }}

</div>