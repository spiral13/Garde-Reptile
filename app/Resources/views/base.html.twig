<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
		<link rel="stylesheet" href="rAssets/css/app.css">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootswatch/4.1.0/minty/bootstrap.min.css">
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>

    {# NAV BAR #}
    <nav class="navbar navbar-expand navbar-light bg-primary ">
        <a class="navbar-brand mr-5" href="{{ path('homepage') }}">REPTIGARDE</a>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item ">
                    <a class="nav-link mr-2" href="{{ path('homepage') }}">Accueil</a>
                </li>

    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}

                <li class="nav-item">
                    <a class="nav-link" href="#">Offres</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Demandes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Créer votre annonce</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link ml-3" href="{{ path('fos_user_profile_show') }}">Mon profil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link ml-3" href="{{ path('fos_user_security_logout') }}">Déconnexion</a>
                </li>

    {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('fos_user_registration_register') }}">Inscription</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('fos_user_security_login') }}">Connexion</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#"></a>
                </li>

    {% endif %}
            </ul>
        </div>
     </nav>
    {# END NAVBAR #}



    {#<div>#}
        {#{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}#}
            {#{{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }} |#}
            {#<a href="{{ path('fos_user_security_logout') }}">#}
                {#{{ 'layout.logout'|trans({}, 'FOSUserBundle') }}#}
            {#</a>#}
        {#{% else %}#}
            {#<a href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>#}
        {#{% endif %}#}
    {#</div>#}

        {% block body %}{% endblock %}

        <div id="root"></div>


    {% if is_granted('ROLE_ADMIN') %}
        <div class="fixed-bottom">
            <a class="btn btn-danger btn-sm m-2" href="{{ path('easyadmin') }}">Interface Admin</a>
        </div>
    {% endif %}


    {% block javascripts %}{% endblock %}
		<script src="rAssets/js/app.js"></script>
		<script>require('src/index');</script>
    </body>
</html>
