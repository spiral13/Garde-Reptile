{% extends 'base.html.twig' %}

{% block body %}

    <div class="container form-group m-5">
    {{ form_start(form) }}
        <div class="form-group col-4 ">{{ form_row(form.comment, {'attr': {'class':'form-control'}})  }} </div>

        <button class="btn btn-primary" type="submit"> Commenter </button>
    {{ form_end(form) }}
    </div>
{% endblock %}