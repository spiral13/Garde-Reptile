{% extends 'base.html.twig' %}

{% block body %}

<div class="container border mt-5 " id="addetail">

    <h3>{{ ad.title }}</h3>

    <p><a href="{{ path('profile_id', {'id': ad.user.id }) }}">Publiée par : {{ ad.user.username}} <span class="text-secondary">le {{ ad.date|date("d/m/Y") }}</span></a> </p>

    <p>Ville : {{ ad.ville }}</p>
    <p>{{ ad.content }}</p>
    <p>Service : {{ ad.service}} de garde</p>
    <a href="#"><button class="mt-3 mb-3 btn btn-danger">Contacter {{ ad.user.username }}</button></a>
</div>


    <a href="{{ path('ad_comment', {'id': ad.id}) }}"><button class="mt-5 mb-3 mr-3 btn btn-info float-right">Laisser un commentaire</button></a>
    <hr class="mt-4">
    <h4 class="ml-5 mt-3"> Vos commentaires :</h4>

    {#{{ dump(app.user) }}#}
    {#{{ dump(ad.user) }}#}


{% endblock %}