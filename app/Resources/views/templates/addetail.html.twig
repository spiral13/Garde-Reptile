{% extends 'base.html.twig' %}

{% block body %}

<div class="container border mt-5 ">

    <h3>Titre = {{ ad.title }}</h3>
    <p>Créer par : {{ ad.user.username }}</p>
    <p>Ville = {{ ad.ville }}</p>
    <p>Contenu = {{ ad.content }}</p>
    <p>Service = {{ ad.service}}</p>
</div>

    <hr class="mt-5">
    <h4 class="ml-5 mt-3"> Vos commentaires :</h4>

    {#{{ dump(formCom) }}#}
    {#<div>#}
        {#{{ form_start(formCom) }}#}
        {#{{ form_widget(formCom) }}#}
        {#{{ form_end(formCom) }}#}
    {#</div>#}
{% endblock %}