{% extends 'base.html.twig' %}

{% block body %}

    <h3>Page Create Ad</h3>

    <div class="container mt-2 p-4 border rounded">
        {{form_start(form)}}

        {{ form_widget(form) }}

        <button type="submit"> Publier </button>

        {{ form_end(form) }}
    </div>


{% endblock %}